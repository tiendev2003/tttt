import 'package:flutter/material.dart';

class ProfessorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Professor Screen'),
      ),
      body: const Center(
        child: Text('This is the Professor Screen'),
      ),
    );
  }
}
